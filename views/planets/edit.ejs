<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit <%= planet.name %></title>
</head>
<body>
  <h1>Edit <%= planet.name %></h1>
  <form action="/planets/<%= planet._id %>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" value="<%= planet.name %>" required>
    
    <label for="description">Description:</label>
    <input type="text" name="description" value="<%= planet.description %>" required>

    <label for="hasLife">Has Life:</label>
    <input type="checkbox" name="hasLife" <% if (planet.hasLife) { %> checked <% } %> >

    <button type="submit">Update Planet</button>
  </form>
  <a href="/planets/<%= planet._id %>">Cancel</a>
</body>
</html>
